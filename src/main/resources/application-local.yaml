spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/arenax
    username: arenax
    password: arenax_dev_password
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

application:
  security:
    jwt:
      secret: ${JWT_SECRET:arenax-local-development-jwt-secret-key-please-change}

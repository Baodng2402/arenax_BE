# Test messaging config: producer + consumer -> disable both relay and listener
arenax:
  messaging:
    relay:
      enabled: false

spring:
  rabbitmq:
    listener:
      simple:
        auto-startup: false
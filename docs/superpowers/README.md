# docs/superpowers

> **Nội bộ (planning artifacts)** — Thư mục này chứa các plan/spec nội bộ dùng trong quá trình phát triển, không phải tài liệu sản phẩm. Đọc tổng quan repo tại [docs/overview.md](../overview.md).

Các file ở đây là lịch sử lập kế hoạch (`plans/`, `specs/`) và không nằm trên primary reading path.